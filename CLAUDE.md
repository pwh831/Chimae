# 프로젝트 규칙

초기·중기 치매 환자를 위한 개인 맞춤형 회상 게임.
상세 기획은 `docs/PRD.md`, 개발 순서는 `docs/프롬프트.md` 참고.

---

## 이 앱이 무엇인가

가족·요양보호사가 입력한 환자의 생애 정보로 AI가 맞춤 회상 활동을 만들고, 손자녀와 메시지를 주고받는 앱.
**1차 사용자는 초기·중기 치매를 겪는 어르신이며, 혼자 손가락으로 조작한다.**

치료·진단 서비스가 아니라 정서적 안정과 소통 지원 서비스다.

---

## 절대 어기면 안 되는 원칙

### 1. 정답을 판정하지 않는다
회상 활동의 목적은 정답을 맞히는 것이 아니라 기억을 꺼내는 경험이다.

- 오답 표시, X 아이콘, 빨간색 경고, 실패 화면을 만들지 않는다
- 어떤 답을 골라도 따뜻하게 받아주고 다음으로 넘어간다
- **점수, 코인, 별점, 정답률, 랭킹, 연속 기록(스트릭)을 만들지 않는다**
- 요청받지 않아도 "게임성을 높이려고" 이런 요소를 추가하지 말 것

### 2. 경쟁 요소를 넣지 않는다
인지 수준 차이가 드러나면 뒤처진 사람이 위축된다. 개인 순위·기여도 비교는 어떤 형태로도 표시하지 않는다.

### 3. 사용을 유도하지 않는다
- "한 번 더 하기", "계속하기", "오늘 목표를 채우세요" 같은 유도 문구 금지
- 세션은 "내일 또 이야기해요"로 자연스럽게 끝난다
- 알림·배지로 재방문을 압박하지 않는다

### 4. 의료적 표현을 쓰지 않는다
| 금지 | 사용 |
| --- | --- |
| 치매를 치료/예방한다 | 정서적 안정과 소통을 돕는다 |
| 인지 기능을 향상시킨다 | 인지 활동을 이어가도록 돕는다 |
| 상태를 진단/판정/평가한다 | 활동 기록을 가족과 공유한다 |

UI 문구, 주석, 변수명 어디에도 진단·치료를 암시하는 표현을 쓰지 않는다.

### 5. AI가 사람을 대체하지 않는다
- AI가 사람인 척하는 연출 금지
- 대화 중 가족에게 이야기하도록 자연스럽게 유도한다
- 하루 대화 분량에 자연스러운 마무리를 둔다

---

## UI 기준 (어르신용 화면)

- 기본 글자 24px 이상, 버튼 안 글자 32px 이상
- 터치 영역 최소 100x100px, 버튼 사이 간격 넉넉히
- 흰 배경 + 진한 회색 글자, 명도 대비 크게
- **화면당 누를 수 있는 요소 최대 4개**
- 색으로만 정보를 전달하지 않기 (아이콘·글자 병행)
- 애니메이션은 느리고 부드럽게, 깜빡임·빠른 움직임 금지
- 드래그앤드롭 사용 금지 (탭으로만 조작)
- 타이핑 입력 요구 금지 (음성 또는 선택지)
- 뒤로 가기 버튼 없음 (길을 잃는다)
- 기준 해상도: 태블릿 가로 1280x800

가족용 화면(8단계)은 이 기준을 따르지 않아도 된다. 일반적인 모바일 앱 디자인으로 만든다.

---

## 작업 방식

### 한 번에 하나씩
- 한 요청 = 한 화면 또는 한 기능
- **요청받은 파일 외에는 수정하지 않는다.** 개선 아이디어가 있으면 코드를 고치지 말고 말로 제안한다
- 요청하지 않은 기능을 미리 만들지 않는다

### 범위를 좁게 유지한다
결선 시연 요건은 **핵심 플레이 1개, 3~5분**이다. 3명이 3개월 안에 만들 수 있는 범위로 유지하는 것 자체가 대회 심사 항목이다.
기능을 늘리자는 제안보다 지금 것을 안정적으로 만드는 쪽을 우선한다.

### 시연 안정성 우선
- API 호출 실패, 네트워크 끊김, 마이크 권한 거부 시에도 앱이 멈추지 않아야 한다
- 오류 메시지를 어르신 화면에 띄우지 않는다. 조용히 대체 동작으로 넘어간다
- 오프라인에서도 전체 흐름이 돌아가야 한다

---

## 기술 스택

- React + Vite + TypeScript
- 상태 관리는 useState / Context로 충분 (별도 라이브러리 도입하지 않기)
- 스타일은 프로젝트에 이미 잡힌 방식을 따른다
- 브라우저 저장소 대신 메모리 상태 사용 (데모 범위)
- AI 연동은 9단계 전까지 mock 데이터로 진행

---

## 데이터 취급

개인 생애사·가족관계·건강 관련 정보를 다룬다.

- 실제 개인정보를 코드나 샘플 데이터에 넣지 않는다. 모두 가상 인물로
- 민감 주제 플래그가 있는 소재는 활동 생성에서 우선순위를 낮춘다
- 어르신 본인이 자신의 기록이 가족과 공유됨을 아는 구조를 유지한다

---

## 커밋

아래 지점에서 커밋한다.

1단계(데이터 구조) / 5단계(회상 활동) / 7단계(전체 연결, 최소 시연 가능) / 9단계(AI 연동) / 11단계(시연 준비)

커밋 메시지는 한국어로 간결하게.
